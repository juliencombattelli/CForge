cmake_minimum_required(VERSION 3.20)
project(cforge_unit_coverage_test_project)

set(VAR "a;c;b") #[==[ Hello ]==] #[===[
hello
 <- this is not the closing
]

]===]

if(UNIX)
    if(APPLE)
        message("")
    elseif()
        message("")
    else()
        message("")
    endif()
else()
    message("")
endif()

# TODO Add testing option
include(CTest) #[==[ Hello ]==]

#[==[ Hello ]==] #[=[
    message("")
]=]

enable_testing()
#[==[ Hello ]==]
